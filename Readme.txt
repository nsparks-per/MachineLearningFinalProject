Run split files with it pointing to the location of your dataset. This
is setup to split each class into train and test catagories. Then run
Audio_Classification_org.ipynb. SVM and Random Forest should work without
issue. The last classifier depends on the sample length being the same
for all datapoints which was not desirable for my dataset.